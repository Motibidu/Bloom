name: Deploy

on:
  push:
    branches: [main]

jobs:
  build:
    name: Build Docker Images
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build backend image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: false
          tags: bloom-backend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: false
          tags: bloom-frontend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # TODO: Task 023 AWS 인프라 구축 완료 후 활성화
  # Required Secrets: EC2_HOST, EC2_USER, EC2_SSH_KEY,
  #                   DATABASE_URL, DB_USERNAME, DB_PASSWORD,
  #                   JWT_SECRET, AWS_ACCESS_KEY, AWS_SECRET_KEY, AWS_S3_BUCKET
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    needs: build
    if: false  # AWS 인프라 준비 후 제거

    steps:
      - uses: actions/checkout@v4

      # TODO: appleboy/ssh-action으로 EC2 접속
      # - name: Deploy to EC2
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.EC2_HOST }}
      #     username: ${{ secrets.EC2_USER }}
      #     key: ${{ secrets.EC2_SSH_KEY }}
      #     script: |
      #       cd ~/bloom
      #       docker compose pull
      #       docker compose up -d
      #       sleep 30
      #       curl -f http://localhost/api/actuator/health || (docker compose rollback && exit 1)
